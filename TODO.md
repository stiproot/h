# TODO 

## Format
- [ ] example of something to do/check

##
- [ ] Move h steering (`h-runtime.md`) out of `apps/claude-agent/` to a repo-root dir delivered via an env (mirror the `skills/` + `H_SKILLS_DIR` pattern), so it's reusable across agents and the agent service stays lean.
