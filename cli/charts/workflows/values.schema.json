{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$comment": "Shape validation only. Required-ness is enforced per-template with helm's `required` function, so rendering one workflow family never demands another family's values.",
  "type": "object",
  "properties": {
    "agentId": { "type": "string", "minLength": 1 },
    "feature": {
      "type": "object",
      "properties": {
        "slug": {
          "type": "string",
          "pattern": "^$|^[a-z0-9]([a-z0-9-]*[a-z0-9])?$",
          "$comment": "Git-branch-safe: lowercase alphanumerics and hyphens, no leading/trailing hyphen. Empty passes shape validation; the template rejects it via `required`."
        },
        "spec": { "type": "string" },
        "sourceRepo": { "type": "string" },
        "remoteBase": { "type": "string" },
        "models": {
          "type": "object",
          "properties": {
            "plan": { "type": "string" },
            "implement": { "type": "string" }
          },
          "additionalProperties": false
        }
      },
      "additionalProperties": false
    }
  }
}
