{
  "steps": [
    {
      "id": "setup-claude",
      "activity": "setup",
      "input": {
        "agentId": "claude-agent",
        "setup": [
          {
            "cmd": "printf \"{}\" > tessl.json"
          },
          {
            "cmd": "tessl init --agent claude-code"
          },
          {
            "cmd": "mkdir -p .claude && cp $AGENT_APP_DIR/.claude/settings.json .claude/settings.json"
          },
          {
            "cmd": "cp $AGENT_APP_DIR/CLAUDE.md CLAUDE.md"
          },
          {
            "cmd": "cp $AGENT_APP_DIR/.mcp.host.json .mcp.json"
          }
        ]
      }
    },
    {
      "id": "run-with-mcp",
      "activity": "run-claude",
      "input": {
        "task": "Use the mcp__workflows__run_workflow tool to trigger a workflow that runs the dapr-claude-loop-agent.\n\nThe workflow steps should be:\n\n1. A setup step:\n   - activity: \"setup\"\n   - input.agentId: \"dapr-claude-loop-agent\"\n   - input.setup: [{\"cmd\": \"printf \\\"{}\\\" > tessl.json\"}, {\"cmd\": \"tessl init\"}]\n\n2. A run step:\n   - activity: \"run-dapr-claude-loop\"\n   - input.task: \"Use the search_skills tool to find the most relevant skill for implementing a Node.js REST API. Search for something like 'node api' or 'hexagonal architecture node'. Pick the best result based on its description and scores. Install it using install_skill. Once installed, read it with read_skill and use the skill to implement a simple Node.js API in a hex-api/ directory that translates English text to hex — a single POST /translate endpoint. Also write a one-shot bash script (run-and-test.sh) that starts the server and fires a curl request against it.\"\n\nAfter calling run_workflow, report the instanceId you received back."
      }
    }
  ]
}
