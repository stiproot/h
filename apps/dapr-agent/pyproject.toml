[project]
name = "dapr-agent"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "agent-server",
  "dapr-agents>=1.0.0",
  "uvicorn>=0.30",
  "fastapi>=0.115",
]

[tool.uv.sources]
agent-server = { workspace = true }

[dependency-groups]
dev = ["ruff>=0.9"]

[tool.ruff]
target-version = "py312"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]
