{
  "name": "run-ledger",
  "version": "0.1.0",
  "description": "The run ledger — per-run summary/events/output artifacts plus the optional statestore mirror, shared by every agent host",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc --project tsconfig.build.json",
    "lint": "oxlint src && oxfmt --check src",
    "format": "oxfmt src",
    "test": "node ../../../scripts/check-workspace-built.mjs && vitest run"
  },
  "dependencies": {
    "@effect/platform": "^0.96.2",
    "effect": "^3.21.4"
  },
  "devDependencies": {
    "@effect/platform-node": "^0.107.0",
    "@effect/vitest": "^0.29.0",
    "@types/node": "^24"
  }
}
