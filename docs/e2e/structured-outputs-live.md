# Structured outputs — live validation

This file was created by the structured-outputs e2e chain
(docs/plans/structured-workflow-outputs.md). Its PR was opened by a run whose
final output ended with a machine-validated fenced json block, and the chain
threaded the PR number from that block — not from a ===PR=== marker.
